import React from 'react';
import ReactDOM from 'react-dom/client';
import FavouriteColor from './hooks/useState';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FavouriteColor />
  </React.StrictMode>
);

